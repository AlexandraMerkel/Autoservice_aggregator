%h1
  = t('helpers.editing')
  = Service.model_name.human(count: 2)

= render 'form_edit'

/= link_to t('helpers.show'), admin_service_path(@service), class: 'btn btn-info'

/= link_to t('helpers.back'), admin_services_path